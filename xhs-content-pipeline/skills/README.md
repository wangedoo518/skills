# Legacy Skills

This directory is kept only as historical reference for the original `xhs-content-pipeline` skill drafts.

The active Hermes Skill Pack is:

```text
/Users/champion/Documents/develop/hermes-skills
```

Do not add this `skills/` directory to `~/.hermes/config.yaml` `skills.external_dirs`.
Keeping both directories active causes duplicate skill names such as `xhs-viral-analysis` and unstable routing.
